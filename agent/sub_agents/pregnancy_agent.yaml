# yaml-language-server: $schema=https://raw.githubusercontent.com/google/adk-python/refs/heads/main/src/google/adk/agents/config_schemas/AgentConfig.json
#
# Pregnancy / prenatal specialist sub-agent.
# `instruction` is filled at runtime by the `load_pregnancy_prompt` callback,
# which concatenates agent/prompts/pregnancy_system.md with
# agent/prompts/memory_extraction_guidance.md.

agent_class: LlmAgent
name: pregnancy_specialist
description: >-
  Senior obstetric specialist covering pregnancy, fetal development,
  prenatal care, labor preparation, postpartum recovery, and lactation.
instruction: "{system_prompt}"

tools:
  - name: agent.tools.user_data.fetch_user_persona
  - name: agent.tools.user_data.fetch_daily_logs
  - name: load_memory

# Allow handing control back to the menstrual specialist if the
# conversation shifts off-topic mid-flow.
disallow_transfer_to_peers: false

before_agent_callbacks:
  - name: agent.core.callbacks.load_pregnancy_prompt

# Persist the session to Memory Bank after every turn this specialist
# handles. Required because, once the coordinator transfers control here,
# subsequent turns no longer fire the coordinator's after_agent_callback.
after_agent_callbacks:
  - name: agent.core.callbacks.save_session_to_memory
